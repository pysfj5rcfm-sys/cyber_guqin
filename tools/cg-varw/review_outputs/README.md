# CG-VARW Review Outputs

This directory is for local review-only R0 drafts and CSV exports.

Generated files under `r0/drafts/` and `r0/exports/` are intentionally ignored by git. They are not sample ingest data, not recording segments, not sample assets, not render output, and not ML training data.
