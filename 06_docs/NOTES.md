# Phase 0.1 Notes

Phase 0.1 说明：当前只做 Gesture Ontology v1.1 与 dummy audio skeleton，目标是让《仙翁操》51 个事件跑通最小工程链路。

当前只做 dummy audio。当前不做真实录音切分。当前不做机器学习。

后续 Phase 1 才做真实录音切分。后续 Phase 2 才做 reference_performance 半自动对齐。后续 Phase 3 才做 `qinist_timing_profile.yaml`、`sample_selection_policy` 真实化、`review_taxonomy.yaml`。

机器学习模型训练后置，不进入本轮。
